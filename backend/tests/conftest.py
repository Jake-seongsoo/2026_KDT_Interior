"""
공유 pytest 픽스처 — tests/ 전체 계층에서 사용.

계층별 역할:
  tests/conftest.py          (이 파일) 공유: 캐시 초기화, 파일 바이트
  tests/services/conftest.py           서비스 픽스처: ClaudeService, ImagenService 등
  tests/routers/conftest.py            라우터 픽스처: TestClient, 의존성 오버라이드
"""

import pytest


# ── 캐시 픽스처 ───────────────────────────────────────────────────────────────

@pytest.fixture
def clear_caches():
    """trend_cache·furniture_query_cache를 테스트 전후로 비운다.

    사용법:
        def test_xxx(clear_caches):
            ...
    """
    from core.cache import furniture_query_cache, trend_cache
    trend_cache.clear()
    furniture_query_cache.clear()
    yield
    trend_cache.clear()
    furniture_query_cache.clear()


# ── 파일 바이트 픽스처 ────────────────────────────────────────────────────────

@pytest.fixture
def minimal_jpeg() -> bytes:
    """유효한 최소 JPEG 바이트 (22 bytes, 1×1 픽셀).

    imghdr.what()이 'jpeg'를 반환하므로 _validate_file을 통과한다.
    TestClient 통합 테스트에서 파일 업로드가 필요할 때 사용.
    """
    return bytes([
        0xFF, 0xD8, 0xFF, 0xE0, 0x00, 0x10, 0x4A, 0x46,
        0x49, 0x46, 0x00, 0x01, 0x01, 0x00, 0x00, 0x01,
        0x00, 0x01, 0x00, 0x00, 0xFF, 0xD9,
    ])


@pytest.fixture
def minimal_png() -> bytes:
    """유효한 최소 PNG 바이트 (67 bytes, 1×1 픽셀).

    PNG signature(0x89504E47)로 시작해 imghdr.what()이 'png'를 반환한다.
    """
    return bytes([
        0x89, 0x50, 0x4E, 0x47, 0x0D, 0x0A, 0x1A, 0x0A,
        0x00, 0x00, 0x00, 0x0D, 0x49, 0x48, 0x44, 0x52,
        0x00, 0x00, 0x00, 0x01, 0x00, 0x00, 0x00, 0x01,
        0x08, 0x02, 0x00, 0x00, 0x00, 0x90, 0x77, 0x53,
        0xDE, 0x00, 0x00, 0x00, 0x0C, 0x49, 0x44, 0x41,
        0x54, 0x08, 0xD7, 0x63, 0xF8, 0xCF, 0xC0, 0x00,
        0x00, 0x00, 0x02, 0x00, 0x01, 0xE2, 0x21, 0xBC,
        0x33, 0x00, 0x00, 0x00, 0x00, 0x49, 0x45, 0x4E,
        0x44, 0xAE, 0x42, 0x60, 0x82,
    ])
