# MVP 로드맵

> 작성일: 2026-05-12 | 기준: GitHub 제출, 8월 마감, 하루 2~3시간, Claude Code + Codex 바이브코딩

---

## 기한 & 여력 분석

| 항목 | 내용 |
|------|------|
| 남은 기간 | 약 12주 (2026-05-12 ~ 2026-08월) |
| 일일 투자 시간 | 2~3시간 |
| 총 가용 시간 | 약 170~250시간 |
| 개발 방식 | Claude Code + Codex 바이브코딩 (체감 속도 2~3배) |
| 제출 방식 | GitHub 업로드 (라이브 데모 없음) |

---

## Phase 1 — 핵심 AI 파이프라인 (4~5주, 필수)

> 목표: 업로드 → 분석 → 톤 선택 → SVG + 방별 이미지 + 추천 근거가 실제로 동작하는 엔드투엔드 플로우 완성

### 백엔드

| 기능 ID | 기능명 | 세부 내용 |
|---------|--------|----------|
| F001 | 도면 업로드 | multipart/form-data, 5MB 제한, GCS 저장 + Signed URL |
| F002 | Claude Vision 방 분석 | 방 이름·개수·우선순위 추출, POST /analyze |
| F004 | 톤 후보 6개 생성 | Claude Web Search 트렌드 캐시(24h TTLCache) + 공간 특성 기반 6개 생성 |
| F004-B | SVG 2D 탑뷰 배치도 생성 | svgwrite, 방 이름·비율 표시, POST /render 내 동기 처리 |
| F004-B | Imagen 방별 렌더링 | Vertex AI Imagen 4, 최대 4개 방 asyncio.gather 병렬, return_exceptions=True |
| F005 | 네이버쇼핑 상품 검색 | 방별 3~5개, 병렬 호출 |
| RISK-01 | gather fallback 처리 | Imagen 부분 실패 시 나머지 반환 보장 |
| RISK-02 | 도면 이미지 보안 | GCS Signed URL(15분 TTL), public URL 반환 금지 |
| RISK-03 | JWT 인증 | /analyze, /render, /share 모두 Depends(verify_jwt) 적용 |

### 프론트엔드

| 페이지 | 구현 내용 |
|--------|----------|
| 도면 업로드 페이지 | 드래그앤드롭, 공급면적 입력, 미리보기 |
| AI 분석 대기 페이지 | 단계별 진행 상태 표시 (분석 중 → 톤 후보 생성 중) |
| 분석 결과 & 톤 선택 페이지 | 방 정보 카드 + 톤 6개 선택 카드 UI |
| 방별 시안 생성 대기 페이지 | 단계별 진행 상태 표시 (배치도 → 이미지 → 상품) |
| 방별 시안 결과 페이지 | SVG 배치도 + 방 탭 + 이미지 + 추천 근거 + 상품 카드 |

### 인증

| 기능 ID | 기능명 | 세부 내용 |
|---------|--------|----------|
| F010 | Google OAuth | Supabase Auth, 도면 업로드·AI 분석 전 로그인 필수 |

---

## Phase 2 — 완성도 & 법적 필수 (3~4주)

> 목표: 실사용 가능한 수준의 UX 완성 + 개인정보·AI기본법 준수

| 기능 ID | 기능명 | 세부 내용 |
|---------|--------|----------|
| F003 | 방 정보 수정 | 인라인 텍스트 수정 + 재분석 버튼 |
| F006 | 톤 재선택 | 결과 페이지 → 분석 결과 & 톤 선택 페이지 복귀 (방 분석 캐시 재사용) |
| F011 | 분석 기록 조회 | 로그인 사용자 이전 결과 목록 + 재열람 |
| F008 | 공유 링크 | share_token 발급, /share/[uuid] SSR + OG 메타태그 |
| RISK-07 | 개인정보 수집 동의 | 업로드 전 ConsentModal (최초 1회), Supabase 메타데이터 저장 |
| RISK-08 | AI 생성 이미지 고지 | AiDisclosure 컴포넌트 (AI기본법 2026.1.22 시행), PIL 워터마크 삽입 |

---

## Phase 3 — 옵션 (남은 시간)

> 목표: 포트폴리오 인상 및 개인 사용 편의성 향상

| 기능 ID | 기능명 | 세부 내용 | 우선순위 |
|---------|--------|----------|----------|
| F007 | 정밀화 맞춤 렌더링 | 예산·가족형태·취향 입력 모달 → 같은 톤 재렌더링 | 높음 |
| - | 랜딩 페이지 polish | 서비스 소개, AI 파이프라인 흐름 시각화 | 높음 |
| F009 | 좋아요/싫어요 피드백 | 방별 시안 👍/👎 수집 | 낮음 |
| F008 | PDF 저장 | 결과 PDF 다운로드 (복잡도 높음) | 낮음 |
| RISK-05 | Cold Start 대응 | 발표·데모 전 /health 워밍업, min-instances=1 임시 설정 | 필요 시 |

---

## 개발 시작 권장 순서

```
1. 백엔드 뼈대
   main.py + CORS + 환경변수(config.py) + JWT 미들웨어(auth.py)

2. POST /analyze
   GCS 업로드 → Claude Vision → 톤 후보 6개 생성 → 응답

3. POST /render
   SVG 생성 → Imagen 병렬 렌더링 → Naver 상품 → DB 저장 → 응답

4. 프론트엔드 핵심 플로우
   업로드 페이지 → 대기 페이지 → 톤 선택 페이지 → 결과 페이지

5. Supabase Auth (Google OAuth)

6. Phase 2 기능 순차 추가
```

---

## 완료 기준 (Definition of Done)

- **Phase 1 완료** = 실제 아파트 도면 JPG를 업로드하면 방별 Imagen 이미지와 SVG 배치도, 추천 근거 텍스트가 결과 페이지에 표시된다
- **Phase 2 완료** = 로그인 후 이전 결과를 재열람할 수 있고, 공유 링크가 동작하며, 개인정보 동의 및 AI 고지가 표시된다
- **Phase 3 완료** = 정밀화 모달과 랜딩 페이지가 완성되어 포트폴리오로 제출 가능한 상태다

---

## 관련 문서

| 문서 | 내용 |
|------|------|
| `.claude/docs/PRD.md` | 기능 명세 및 페이지별 상세 |
| `docs/시스템_아키텍처.md` | API 설계, 코드 구조, 배포 방식 |
| `docs/위험_병목_체크리스트.md` | RISK-01~11 코드 수준 체크리스트 |
| `docs/기술스택_결정.md` | 기술 선택 근거 |
